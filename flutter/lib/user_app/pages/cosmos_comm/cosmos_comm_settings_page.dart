import 'package:flutter/material.dart';

class CosmosCommSettingsPage extends StatelessWidget {
  const CosmosCommSettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('宇宙传讯设置')),
      body: const Center(child: Text('Cosmos Comm Settings Page')),
    );
  }
}
