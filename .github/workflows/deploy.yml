name: fastapi后端部署

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "部署原因"
        required: false
        default: "手动触发部署"

env:
  IMAGE_BASE: ghcr.io/${{ github.repository }}/bipupu-backend

jobs:
  deploy:
    name: 🏗️ 编译并部署
    runs-on: ubuntu-latest
    environment: dev
    permissions:
      contents: read
      packages: write

    env:
      # 全局注入敏感配置，确保部署、健康检查和日志查看的所有步骤都能访问
      SECRET_KEY: ${{ secrets.SECRET_KEY }}
      POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
      ADMIN_EMAIL: ${{ secrets.ADMIN_EMAIL }}
      ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD }}
      ADMIN_USERNAME: ${{ secrets.ADMIN_USERNAME }}

    steps:
      - name: 📥 获取源代码
        uses: actions/checkout@v4

      - name: 🛠️ 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🔐 登录 GitHub 镜像仓库
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🏷️ 生成版本标签
        id: ver
        run: |
          TAG_FINAL="${GITHUB_SHA:0:8}"
          echo "BACKEND_IMAGE=${{ env.IMAGE_BASE }}:$TAG_FINAL" >> "$GITHUB_OUTPUT"
          echo "TAG=$TAG_FINAL" >> "$GITHUB_OUTPUT"

      - name: 📦 构建并推送镜像
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: |
            ${{ steps.ver.outputs.BACKEND_IMAGE }}
            ${{ env.IMAGE_BASE }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: 🔑 配置 SSH 密钥 (直接文件模式)
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SERVER_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          
          # 扫描指纹
          ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts
          
          # 使用 EOF 语法一次性写入 config，更整洁
          cat >> ~/.ssh/config <<EOF
          Host remote-server
              HostName ${{ secrets.SERVER_HOST }}
              User ${{ secrets.SERVER_USER }}
              IdentityFile ~/.ssh/id_rsa
              StrictHostKeyChecking no
              ServerAliveInterval 60
          EOF
          
          echo "Testing SSH Connection..."
          ssh remote-server "echo '✅ SSH Connection OK'"

      - name: 🌐 激活远程 Docker 上下文
        run: |
          # 使用 config 中定义的 Host 别名
          docker context create remote-context --docker "host=ssh://remote-server"
          # 持久化上下文选择到环境变量，确保后续步骤生效
          echo "DOCKER_CONTEXT=remote-context" >> $GITHUB_ENV

      - name: 🔍 预检查 Secrets 配置
        run: |
          # 检查关键变量是否为空（不打印值）
          if [[ -z "$SECRET_KEY" || -z "$POSTGRES_PASSWORD" || -z "$ADMIN_EMAIL" ]]; then
            echo "::error::关键 Secrets 未设置！请检查 GitHub 仓库 Settings -> Secrets -> Actions"
            echo "缺少: SECRET_KEY, POSTGRES_PASSWORD 或 ADMIN_EMAIL"
            exit 1
          fi
          echo "✅ Secrets 格式检查通过"

      - name: 🚀 执行远程滚动部署
        env:
          # 只需要注入动态生成的镜像 tag，其他 secrets 已在全局 env 中
          BACKEND_IMAGE: ${{ steps.ver.outputs.BACKEND_IMAGE }}
        run: |
          echo "🔑 刷新远程登录状态..."
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

          echo "📡 拉取新镜像并启动服务..."
          docker compose \
            -f docker/docker-compose.yml \
            -f docker/docker-compose.prod.yml \
            up -d --remove-orphans

      - name: 🩺 运行远程健康检查
        run: |
          echo "🔍 正在确认服务可用性..."
          success=false
          for i in {1..10}; do
            # 直接在远程容器中通过 uv 运行健康检查
            if docker exec bipupu-backend uv run python -c "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')" 2>/dev/null; then
              echo "✅ 服务已就绪！"
              success=true
              break
            fi
            echo "⏳ 检查中 ($i/10)..."
            sleep 6
          done

          if [ "$success" != "true" ]; then
            echo "❌ 健康检查失败，输出异常日志："
            docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml logs --tail 50
            exit 1
          fi

      - name: 🧹 清理旧镜像
        run: |
          # 自动删除服务器上超过 24 小时的无用镜像，释放磁盘空间
          docker image prune -f --filter "until=24h"

      - name: 🎉 部署完成通知
        if: success() # 确保只有成功时才记录
        run: |
          echo "🚀 成功部署版本: ${{ steps.ver.outputs.TAG }}"
          # 直接使用别名远程写入
          ssh remote-server "echo ${{ steps.ver.outputs.TAG }} > ~/last_version.txt"