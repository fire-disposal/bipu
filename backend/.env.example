# Bipupu 环境变量配置示例
# 将此文件复制为 .env 文件进行本地开发配置
# 注意：CICD流程已不再依赖此文件，仅用于本地开发参考

# 应用基础配置
DEBUG=True
SECRET_KEY=dev-secret-change-this-in-production
ALLOWED_HOSTS=["*"]

# 默认管理员账号（仅用于初始设置）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# ======================
# 数据库配置 (PostgreSQL)
# ======================

# 方式1：使用完整的DATABASE_URL（优先级最高）
# DATABASE_URL=postgresql://username:password@localhost:5432/bipupu

# 方式2：使用单独的配置项
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=bipupu
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# ======================
# Redis配置（可选）
# ======================
# 如果Redis不可用，系统会自动使用内存缓存

# 方式1：使用完整的REDIS_URL（优先级最高）
# REDIS_URL=redis://localhost:6379/0

# 方式2：使用单独的配置项
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password

# ======================
# Celery配置（可选）
# ======================
# 如果不需要Celery任务队列，可以忽略以下配置

# CELERY_BROKER_URL=redis://localhost:6379/1
# CELERY_RESULT_BACKEND=redis://localhost:6379/2

# ======================
# 其他配置
# ======================

# 应用端口
PORT=8000

# 应用环境：development, testing, production
APP_ENV=development

# 容器角色（用于多容器部署）
# CONTAINER_ROLE=backend

# 日志级别：DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# 文件上传配置
MAX_FILE_SIZE=10485760  # 10MB in bytes
UPLOAD_DIR=uploads

# 时区配置
TIMEZONE=Asia/Shanghai

# ======================
# 使用说明
# ======================
# 1. 本地开发：复制此文件为 .env，根据需要修改配置
# 2. Docker部署：通过docker-compose.yml的环境变量配置
# 3. 生产环境：通过容器环境变量或Kubernetes Secrets配置
# 4. Redis为可选服务，连接失败时会自动使用内存缓存
# 5. 数据库必须使用PostgreSQL，SQLite回退功能已移除
